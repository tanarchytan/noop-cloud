{
  "name": "noop-cloud-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "noop-cloud API — Fastify/Node LTS port of the verified Python service (same wire contract).",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch",
    "test": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/static": "^8.1.1",
    "@fastify/swagger": "^9.4.2",
    "@fastify/swagger-ui": "^5.2.2",
    "@sinclair/typebox": "^0.34.33",
    "better-sqlite3": "^12.2.0",
    "fastify": "^5.12.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  }
}
