# Local env / secrets
.env
*.env
# Python
__pycache__/
*.pyc
# Scraper runtime output (regenerated); the seed firmware/firmware-latest.json IS tracked
/data/

# Node
node_modules/
dist/

# Stray local SQLite (the real store lives in the gitignored /data volume)
*.db
*.db-shm
*.db-wal
